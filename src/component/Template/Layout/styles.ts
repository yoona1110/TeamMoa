import styled from "styled-components";

export const DefaultLayout = styled.div`
  width: 100vw;
  height: 100%;

  & > button {
    position: fixed;
    right: 1.5rem;
    bottom: 6.438rem;
    width: max-content;
    height: max-content;
  }
`;
